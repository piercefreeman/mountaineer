[project]
name = "benchmarking"
version = "0.1.0"
description = "Mountaineer benchmarking scripts"
readme = "README.md"
authors = [
     { name = "Pierce Freeman", email = "pierce@freeman.vc" }
]
requires-python = ">=3.10,<3.14"
dependencies = [
    "locust>=2.33.1",
]
